# Debugging API Tests

## Description

This project demonstrates debugging skills in Python API testing using `pytest` and `requests`.

The project contains intentionally broken API tests. The goal was to analyze error messages, identify issues, fix the bugs, and verify that all tests pass successfully.

## Technologies

- Python
- Pytest
- Requests
- REST API testing

## Installation

Clone the repository:

```bash
git clone <repository-url>